export declare class FilterIterator<T> implements Iterator<T> {
    private source;
    private predicate;
    constructor(source: Iterator<T>, predicate: (element: T) => boolean);
    next(): IteratorResult<T>;
}
//# sourceMappingURL=filter.d.ts.map