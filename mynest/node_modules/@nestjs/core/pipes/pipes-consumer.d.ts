import { ArgumentMetadata, Transform } from '@nestjs/common/interfaces';
export declare class PipesConsumer {
    private readonly paramsTokenFactory;
    apply<TInput = any>(value: TInput, { metatype, type, data }: ArgumentMetadata, transforms: Transform<any>[]): Promise<TInput>;
    applyPipes<TInput = any>(value: TInput, { metatype, type, data }: {
        metatype: any;
        type?: any;
        data?: any;
    }, transforms: Transform<any>[]): Promise<TInput>;
}
