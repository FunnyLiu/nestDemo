"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const injector_1 = require("../injector/injector");
class MiddlewareResolver {
    constructor(middlewareContainer) {
        this.middlewareContainer = middlewareContainer;
        this.instanceLoader = new injector_1.Injector();
    }
    async resolveInstances(module, moduleName) {
        const middleware = this.middlewareContainer.getMiddlewareCollection(moduleName);
        await Promise.all([...middleware.values()].map(async (wrapper) => this.resolveMiddlewareInstance(wrapper, middleware, module)));
    }
    async resolveMiddlewareInstance(wrapper, middleware, module) {
        await this.instanceLoader.loadMiddleware(wrapper, middleware, module);
    }
}
exports.MiddlewareResolver = MiddlewareResolver;
